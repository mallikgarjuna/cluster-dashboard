import { Metadata } from "next";
import React from "react";

const CoursesPage = () => {
  return <div>CoursesPage</div>;
};

export const metadata: Metadata = {
  title: "Cluster Dashboard - Courses",
  description: "View all courses",
};

export default CoursesPage;
